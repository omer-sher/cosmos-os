# Copy this to .env.local and fill in your Anthropic API key to enable the local chat.
# The chat is only shown in dev mode (npm run dev) — never in production builds.
VITE_ANTHROPIC_API_KEY=sk-ant-...
